<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Actor</title>
    <link rel='stylesheet' href='/css/style.css' />
</head>

<body>
    <figure>
        <img src="/img/logo-DH.png" alt="Logo Digital House">
      </figure>

    <h1>EDITAR ACTOR</h1>
    <form action="/actors/update/<%= actor.id %>" method="POST">
        <div>
            <label for="first_name">Nombre:</label>
            <input type="text" name="first_name" id="first_name" value="<%= oldData ? oldData.first_name : actor.first_name %>">
            <%= errors.find(error => error.param === 'first_name') ? '<p>' + errors.find(error => error.param === 'first_name').msg + '</p>' : '' %>
        </div>
        <div>
            <label for="last_name">Apellido:</label>
            <input type="text" name="last_name" id="last_name" value="<%= oldData ? oldData.last_name : actor.last_name %>">
            <%= errors.find(error => error.param === 'last_name') ? '<p>' + errors.find(error => error.param === 'last_name').msg + '</p>' : '' %>
        </div>
        <div>
            <label for="rating">Rating:</label>
            <input type="number" step="0.1" name="rating" id="rating" value="<%= oldData ? oldData.rating : actor.rating %>">
            <%= errors.find(error => error.param === 'rating') ? '<p>' + errors.find(error => error.param === 'rating').msg + '</p>' : '' %>
        </div>
        <div>
            <label for="favorite_movie_id">ID Película Favorita:</label>
            <input type="number" name="favorite_movie_id" id="favorite_movie_id" value="<%= oldData ? oldData.favorite_movie_id : actor.favorite_movie_id %>">
            <%= errors.find(error => error.param === 'favorite_movie_id') ? '<p>' + errors.find(error => error.param === 'favorite_movie_id').msg + '</p>' : '' %>
        </div>
        <button type="submit">Actualizar</button>
    </form>
    <a class="botonVolver" href="/actors">Volver al listado</a>
</body>

</html>
