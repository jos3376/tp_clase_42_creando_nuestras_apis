<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= actor.first_name %> <%= actor.last_name %></title>
    <link rel='stylesheet' href='/css/actorDetail.css' />
</head>
<body>
    <figure>
        <img src="/img/logo-DH.png" alt="Logo Digital House">
    </figure>
    <h1><%= actor.first_name %> <%= actor.last_name %></h1>
    <br>
    <p>RATING: <%= actor.rating %></p>
    <p>PELÍCULA FAVORITA: 
        <% if (actor.favorite_movie) { %>
            <a href="/movies/detail/<%= actor.favorite_movie.id %>"><%= actor.favorite_movie.title %></a>
        <% } else { %>
            No disponible
        <% } %>
    </p>
    <br>
    <section class="button-section">
        <!-- Aquí deben colocarse los botones de Modificar - Borrar y Listado de Actores -->
        <a class="botonModificar" href="/actors/edit/<%= actor.id %>">Modificar</a>
        <a class="botonBorrar" href="/actors/delete/<%= actor.id %>">Borrar</a>
        <a class="botonAgregar" href="/actors/add">Agregar</a>
        <a class="botonVolver" href="/actors">Listado de Actores</a>
    </section>
</body>
</html>

